
	// Place your snippets for python here. Each snippet is defined under a snippet name and has a prefix, body and 
	// description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
	// same ids are connected.
	// Example:
	// "Print to console": {
	// 	"prefix": "log",
	// 	"body": [
	// 		"console.log('$1');",
	// 		"$2"
	// 	],
	// 	"description": "Log output to console"
	// }
	{
		"Python Regex Pattern (Named Verbose)": {
			"prefix": "pat",
			"body": [
				"${1:pattern_name} = re.compile(r\"\"\"$2\"\"\", re.VERBOSE)$0"
			],
			"description": "Defines a named Python regex pattern with VERBOSE flag"
		},
// python IO
		"Python open file and read lines": {
				"prefix": "openread",
				"body": [
					"with open(${1:filename_var}) as ${2:file_alias}:",
					"\tfor ${3:line} in ${2:file_alias}:",
					"\t\t# print(${3:line})$0"
				],
				"description": "Opens a file and iterates through its lines"
			
		},
	 
			"Python open file for writing": {
				"prefix": "openwrite",
				"body": [
					"with open('${1:filename}', 'w') as ${2:file_alias}:",
					"\t${2:file_alias}.write('${3:content}')",
					"\t$0"
				],
				"description": "Opens a file in write mode ('w')"
			},
			"Python open file for appending": {
				"prefix": "openappend",
				"body": [
					"with open('${1:filename}', 'a') as ${2:file_alias}:",
					"\t${2:file_alias}.write('${3:content}')",
					"\t$0"
				],
				"description": "Opens a file in append mode ('a')"
			}
		
	 // add new ones here 
		"Python f-string print": {
			"prefix": "pffp",
			"body": [
				"print(f\"   {${1:var_name}} \")"
			],
			"description": "Prints an f-string with leading/trailing spaces"
		},
		"Python f-string print with var": {
			"prefix": "pff",
			"body": [
				"print(f\" ${1:var_name} = {$1} \")"
			],
			"description": "Prints an f-string with var"
		},
		
	}